[Unit]
Description=NATS Streaming Server
After=network.target

[Service]
StandardOutput=syslog
StandardError=syslog
User=root
Group=root
ExecStart={{bindir}/{{pkgname}} --config={{etcdir}}/{{pkgname}}.conf

[Install]
WantedBy=multi-user.target
